{
  "name": "wordfinder",
  "version": "0.0.46",
  "private": true,
  "description": "Playwright test harness for the WordFinder static web app",
  "scripts": {
    "test": "playwright test",
    "test:all": "./run-all-tests.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
